diff --git a/CMakeLists.txt b/CMakeLists.txt
index 5692e44..ca9e39d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -50,6 +50,7 @@ option(OPENCC_ENABLE_INSTALL "Generate install targets" ${_OPENCC_DEFAULT_ENABLE
 option(ENABLE_GTEST "Build all tests." OFF)
 option(ENABLE_BENCHMARK "Build benchmark tests." OFF)
 option(ENABLE_DARTS "Build DartsDict (ocd format)." ON)
+option(ENABLE_PLUGINS "" ON)
 option(BUILD_OPENCC_JIEBA_PLUGIN "Build Jieba segmentation as a loadable plugin." OFF)
 option(BUILD_PYTHON "Build python library" OFF)
 option(USE_SYSTEM_DARTS "Use system version of Darts" OFF)
@@ -287,10 +288,12 @@ endif()
 ######## Subdirectories
 
 add_subdirectory(src)
-add_subdirectory(doc)
+if(APPLE AND NOT IOS)
 add_subdirectory(data)
+endif()
+if(ENABLE_PLUGINS)
 add_subdirectory(plugins)
-add_subdirectory(test)
+endif()
 
 if (ENABLE_BENCHMARK AND TARGET performance AND TARGET opencc_jieba)
   target_compile_definitions(performance PRIVATE
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index d4edff8..1dfcd15 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -33,7 +33,6 @@ set(
   MarisaDict.hpp
   MaxMatchSegmentation.hpp
   Optional.hpp
-  PluginSegmentation.hpp
   PhraseExtract.hpp
   PrefixMatch.hpp
   Segmentation.hpp
@@ -61,7 +60,6 @@ set(
   Lexicon.cpp
   MarisaDict.cpp
   MaxMatchSegmentation.cpp
-  PluginSegmentation.cpp
   PhraseExtract.cpp
   PrefixMatch.cpp
   SerializedValues.cpp
@@ -72,6 +70,11 @@ set(
   UTF8Util.cpp
 )
 
+if (ENABLE_PLUGINS)
+  list(APPEND LIBOPENCC_HEADERS PluginSegmentation.hpp)
+  list(APPEND LIBOPENCC_SOURCES PluginSegmentation.cpp)
+endif()
+
 set(UNITTESTS
   ConfigTest
   ConversionChainTest
@@ -139,6 +142,8 @@ target_link_libraries(libopencc marisa)
 if (NOT BUILD_SHARED_LIBS)
   target_compile_definitions(libopencc PUBLIC Opencc_BUILT_AS_STATIC)
 endif()
+if (ENABLE_PLUGINS)
+target_compile_definitions(libopencc PUBLIC OPENCC_ENABLE_PLUGINS)
 # PluginSegmentation.cpp uses LoadLibrary on _WIN32 (MSVC and MinGW).
 # Only non-Windows targets may need libdl for dlopen.
 if (NOT CMAKE_SYSTEM_NAME STREQUAL "Windows")
@@ -148,6 +153,7 @@ if (NOT CMAKE_SYSTEM_NAME STREQUAL "Windows")
     target_link_libraries(libopencc dl)
   endif ()
 endif ()
+endif ()
 target_include_directories(libopencc PUBLIC
     $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
     $<INSTALL_INTERFACE:${DIR_INCLUDE}/opencc>
@@ -204,11 +210,13 @@ install(
   DESTINATION ${DIR_INCLUDE}/opencc
 )
 
+if (ENABLE_PLUGINS)
 install(
   FILES plugin/OpenCCPlugin.h
   DESTINATION ${DIR_INCLUDE}/opencc/plugin
 )
 endif()
+endif()
 
 # Gtest
 
@@ -244,4 +252,6 @@ endif()
 
 # Subdir
 
+if(APPLE AND NOT IOS)
 add_subdirectory(tools)
+endif()
diff --git a/src/Config.cpp b/src/Config.cpp
index 6a80e5d..0ab2838 100644
--- a/src/Config.cpp
+++ b/src/Config.cpp
@@ -35,7 +35,9 @@
 #include "Exception.hpp"
 #include "MarisaDict.hpp"
 #include "MaxMatchSegmentation.hpp"
+#ifdef OPENCC_ENABLE_PLUGINS
 #include "PluginSegmentation.hpp"
+#endif
 #include "TextDict.hpp"
 #include "UTF8Util.hpp"
 
@@ -347,6 +349,7 @@ public:
       // Required: dict
       DictPtr dict = ParseDict(GetObjectProperty(doc, "dict"));
       segmentation = SegmentationPtr(new MaxMatchSegmentation(dict));
+#ifdef OPENCC_ENABLE_PLUGINS
     } else {
       PluginConfigPairs configPairs;
       configPairs.push_back(std::make_pair("__config_dir", configDirectory));
@@ -374,6 +377,13 @@ public:
         configPairs.push_back(std::make_pair(key, it->value.GetString()));
       }
       segmentation = CreatePluginSegmentation(type, configPairs);
+#else
+    } else {
+      throw InvalidFormat(
+          "Segmentation plugin '" + type +
+          "' is not supported. Plugins are disabled (ENABLE_PLUGINS=OFF). "
+          "Only 'mmseg' type is available.");
+#endif
     }
     return segmentation;
   }
